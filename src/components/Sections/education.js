import { h } from 'preact';

/**
 * @param {Object} props
 * @returns {JSX}
 */
export default ({ schools }) => (
    <section class="">
        Did u really expect this one not to be in progress?
    </section>
);
