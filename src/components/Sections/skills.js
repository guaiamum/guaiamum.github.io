import { h } from 'preact';

/**
 * @param {Object} props
 * @returns {JSX}
 */
export default ({ skills, skillsWanted }) => (
    <section class="" style="min-height: 200vh;">
        in progress
    </section>
);
