import { h } from 'preact';

export default (props) => (
    <main class="loading">
        <span class="dot">.</span>
        <span class="dot">.</span>
        <span class="dot">.</span>
    </main>
);
